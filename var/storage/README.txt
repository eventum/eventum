This directory stores attachments if eventum is configured to store on the local filesystem instead of DB.

This directory must be readable and writable for webserver user and the user who runs
Eventum crons.
